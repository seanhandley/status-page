//Set up the modal for the quick edit box
$modal = $('.modal'),
$modalBody = $('.modal .modal-body'),
$modalHeading = $('.modal .modal-heading');
$modalHeading.html("<%= @event.title %>");
$modalBody.html("<%= escape_javascript(render "quick_edit") %>");
$modal.modal();